torch>=2.1.0
numpy>=1.26.0
onnx>=1.15.0
onnxscript>=0.7.0
onnxruntime>=1.16.0
pillow>=10.0.0
opencv-python>=4.8.0
scikit-learn>=1.3.0
tqdm>=4.66.0
# Google Drive downloader for ml/scripts/fetch_semlex.py — used for the
# small metadata CSV. The large video tarballs are stream-downloaded via
# `requests` directly (gdown writes to disk; we can't afford that for the
# 23.7 GB train tarball on a 14 GB runner).
gdown>=5.2.0
requests>=2.31.0
# Product-quality landmark path. This intentionally uses a pretrained hand
# detector; see docs/PRODUCT_MODEL_PIVOT.md.
mediapipe>=0.10.35
